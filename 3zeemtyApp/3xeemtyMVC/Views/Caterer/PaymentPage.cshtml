<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: #ff00cc;
            background: -webkit-linear-gradient(to right, #333399, #ff00cc);
            background: linear-gradient(to right, #333399, #ff00cc);
        }

        .container {
            width: 90%;
            margin: 20px auto;
        }

        .payment-form {
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            padding: 40px;
            margin-top: 50px;
        }

        .form-header {
            padding-bottom: 30px;
            border-bottom: 1px solid #ddd;
            margin-bottom: 30px;
        }

        .title {
            font-size: 24px;
            margin: 0;
            color: #5e6977;
        }

        .form-body {
            padding: 0 20px;
        }

        .card-inputs {
            display: flex;
            margin-bottom: 20px;
        }

            .card-inputs input {
                flex: 1;
                height: 40px;
                padding: 10px;
                border: 2px solid #e1e8ee;
                border-radius: 6px;
                background-color: #f9f9f9;
                font-size: 16px;
                font-weight: 100;
                line-height: 16px;
                color: #333;
                margin-right: 10px;
            }

        .cvv-input {
            width: 80px;
        }

        .paypal-btn {
            cursor: pointer;
            font-size: 16px;
            width: 100%;
            border: none;
            background-color: #ff3366;
            color: #fff;
            padding: 15px;
            border-radius: 6px;
            transition: background-color 0.3s;
            text-decoration: none;
            display: block;
            text-align: center;
        }

            .paypal-btn:hover {
                background-color: #ff4d94;
            }
    </style>
</head>
<body>
    <div class="container">
        <div class="payment-form">
            <div class="form-header">
                <h3 class="title">Payment Information</h3>
            </div>
            <div class="form-body">
                <div class="card-inputs">
                    <input type="text" placeholder="Card Number" value="CARD NUMBER">
                </div>
                <div class="card-inputs">
                    <input type="text" placeholder="MM" value="MM">
                    <input type="text" placeholder="YY" value="YY">
                    <input type="text" class="cvv-input" placeholder="CVV" value="cvv">
                </div>
                <button class="paypal-btn">Pay Now</button>
            </div>
        </div>
    </div>
</body>
</html>
